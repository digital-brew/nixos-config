(defwidget files []
	  (box
	      :orientation "h"
		    :space-evenly false
		    :class "widget files"
  	    (button
  	        :class "files-button"
		   	    :onclick "nemo &"
            files-icon
        )
    )
)