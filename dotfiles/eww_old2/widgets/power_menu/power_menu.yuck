(defwidget power-menu []
	  (box
	      :orientation "h"
		    :space-evenly false
		    :class "widget power-menu"
  	    (button
  	        :class "power-menu-button"
		   	    :onclick "wlogout &"
		   	    :halign "center"
		   	    power-switch-icon
        )
    )
)